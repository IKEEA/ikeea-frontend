import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  warning: {
    fontSize: '12px',
    float: 'right'
  },
  right: {
    float: 'right'
  }
}));