import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    select: {
        width: '300px'
    },
    dateSelect: {
        width: '300px',
        marginTop: '-16px'
    }
  }));


